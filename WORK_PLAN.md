# QuantLab 工作计划

更新日期：2026-06-23

本文档用于跟踪当前本地 A 股量化回测平台的开发状态，包含已完成事项、正在处理事项和未完成路线图。

## 已完成

| 模块 | 状态 | 说明 |
|---|---:|---|
| 本地 Web 架构 | ✅ | FastAPI 后端 + React/Vite 前端，可在本机运行。 |
| A 股日线回测 MVP | ✅ | 支持日线频率，`frequency` 字段保留分钟线扩展入口。 |
| 可视化策略配置 | ✅ | 支持均线/价格均线/RSI 条件组合，买卖条件可视化编辑。 |
| 回测核心规则 | ✅ | 收盘信号、下一交易日开盘成交、T+1、整手交易、交易成本、滑点、最大仓位。 |
| 涨跌停/停牌约束 | ✅ | 支持涨停不买、跌停不卖、停牌不交易；按分比较价格边界。 |
| 数据集快照 | ✅ | CSV/AkShare 数据固化为 SHA-256 指纹快照，支持去重、预览和回测追溯。 |
| AkShare 数据接入 | ✅ | 支持沪深日线、沪深300基准、交易日历、东方财富失败后新浪回退。 |
| CSV 导入 | ✅ | 支持 10MB CSV 导入、OHLCV 校验、交易日过滤和持久化。 |
| 回测任务持久化 | ✅ | SQLite 保存任务、配置、进度、取消状态和结果，重启后可查看历史。 |
| 项目/策略版本 | ✅ | 支持默认项目、策略保存、不可变版本和回测记录关联。 |
| 前端数据中心 | ✅ | 支持演示数据、CSV 快照、AkShare 快照三路切换。 |
| 前端模块化 | ✅ | 已将单文件 `App.jsx` 拆为配置、格式化、通用控件、抽屉/弹窗和仪表盘展示模块，主组件只保留状态与业务动作。 |
| 证券主数据/状态表 | ✅ | SQLite 增加 `securities` 与 `security_daily_status`，导入 CSV/AkShare 快照时抽取上市日、交易所、板块、ST/停牌/涨跌停日状态，回测创建前校验上市/退市窗口。 |
| 复权因子与公司行为校验 | ✅ | 数据快照标准化 `adjust_factor`、`adjusted_close`、`corporate_action`、`adjustment_anomaly`，并持久化数据集复权质量检查；回测结果披露当前“未复权撮合/信号”口径。 |
| 审查报告 2026-06-22 修复 | ✅ | 已处理 ST/8 开头涨跌停、真实名称、CSV 交易日过滤、AkShare 异步同步、涨跌停价格精度、回退列映射和初始化回测取消。 |

## 当前版本验证清单

| 验证项 | 目标 |
|---|---|
| 后端单元测试 | `python -m pytest backend\tests -q` 全部通过。 |
| 前端生产构建 | `npm run build` 通过。 |
| 本地 API | `/api/health` 返回 `ok`。 |
| UI 回归 | 数据选择、策略保存、回测执行、结果展示无控制台错误。 |

## 未完成 / 后续迭代

| 优先级 | 项目 | 说明 |
|---:|---|---|
| P0 | 复权信号口径扩展 | 当前已保留复权字段并做连续性检查；后续可增加“复权价计算信号、未复权价撮合”的策略口径切换。 |
| P0 | 证券主数据版本库增强 | 当前已落主数据与日状态表；后续需接入全市场证券列表、真实上市/退市日、历史行业/板块和长期停牌规则。 |
| P0 | 涨跌停规则版本化 | 当前已覆盖 ST、科创板、创业板注册制后、8 开头 30%；仍需补 IPO 首日/新股前几日无限制等历史例外。 |
| P1 | 回测报告 | 增加 HTML/PDF 报告导出、参数快照、数据指纹、异常披露。 |
| P1 | 结果分析增强 | 交易分页、筛选、订单拒绝统计、多次回测对比。 |
| P1 | 策略能力扩展 | 增加止盈止损、单标的仓位上限、调仓周期、股票池过滤器。 |
| P1 | 前端性能优化 | 增加错误边界、加载状态和 Recharts 按需/懒加载，降低主包体积。 |
| P2 | 分钟线扩展 | 基于已保留的频率字段扩展分钟线数据模型、撮合语义和前端配置。 |
| P2 | 数据源治理 | 增加依赖锁定、数据质量报告、黄金样本和更完整的数据源失败测试。 |
| P2 | 权限与多项目 | 增加多用户/多项目隔离、配置导入导出和团队协作能力。 |

## 近期建议顺序

1. 增强证券主数据来源，接入全市场证券列表、真实上市/退市日和历史状态。
2. 完善涨跌停规则版本化，补 IPO 首日/新股前几日无限制等历史例外。
3. 增加“复权价计算信号、未复权价撮合”的策略口径切换。
4. 增加错误边界、加载状态和图表懒加载，继续压低前端维护与加载成本。
