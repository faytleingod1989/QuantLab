# Code Self Review — 2026-06-23

审查范围：当前 `main` 分支的后端数据/仓储/API/任务流、前端 React 模块和工作计划。

## 结论

当前阶段代码可运行，测试与构建通过；但项目整体尚未完成完整 PRD，仍有 `WORK_PLAN.md` 中列出的后续迭代项。

## 已验证

| 项目 | 结果 |
|---|---|
| 后端测试 | `python -m pytest backend\tests -q` 通过 |
| 前端构建 | `npm run build` 通过 |
| Git 状态 | 自查前为干净同步状态 |
| React 结构 | 已模块化，无组件内嵌定义；首屏核心请求保持并行 |

## 本轮发现并修复

| 级别 | 问题 | 修复 |
|---|---|---|
| P1 | 应用重启时 demo seed 可能把 AkShare 已同步的真实上市日回退为 `1990-12-19`。 | 调整 `upsert_securities` 字段优先级：`akshare_master` 优先于 `demo/seed`，demo seed 不再覆盖真实上市日、状态和来源。 |
| P1 | 北交所 30% 涨跌停只覆盖了 8 开头代码，未覆盖 `920xxx.BJ` / `430xxx.BJ`。 | `_limit_rate` 改为优先按交易所后缀 `.BJ` 判断 30% 规则。 |

## 已补测试

- `test_demo_seed_does_not_regress_akshare_master_listing_date`
- `test_limit_rates_cover_st_star_chinext_and_bj` 覆盖 `920000.BJ`

## 未修复但已记录的后续项

| 项目 | 说明 |
|---|---|
| 前端全市场股票池性能 | 同步全市场后，数据抽屉会一次渲染几千只股票；后续应增加搜索、分页或虚拟列表。 |
| 涨跌停规则版本化 | 仍需补 IPO 首日/新股前几日无限制等历史例外。 |
| 复权信号口径 | 当前已做复权字段与质量检查，尚未开放“复权价算信号、未复权价撮合”的策略口径切换。 |
| 报告导出/结果分页 | 属于后续产品功能，不影响当前核心回测链路。 |

## 当前判断

当前代码适合作为可运行、可追溯的本地 A 股日线回测 MVP 继续迭代；还不应标记为完整生产级量化研究平台。
