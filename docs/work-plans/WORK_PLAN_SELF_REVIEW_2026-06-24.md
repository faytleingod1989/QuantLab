# 2026-06-24 自查修复计划与完成记录

## 已完成

- 修复 `run.bat` 启动路径不稳定问题，改为基于脚本所在目录启动，并显式检查 `backend` 与 `frontend` 目录。
- 修复可视化策略指标切换后沿用错误参数的问题，MACD/布林带/RSI 会自动应用对应默认参数。
- 增加后端策略参数校验，限制短周期必须小于长周期，并校验 MACD 信号周期、布林带倍数、RSI 阈值。
- 修复数据集 CSV/AkShare 导入后数据质量面板不立即刷新的问题。
- 修复行业历史 CSV 导入时证券主表缺失的问题，仅为缺失股票补充最小主表记录，不覆盖已有真实主数据。
- 将“最少上市天数”过滤优先改为上市交易日序号，缺少该字段时才回退自然日。
- 增强 PDF 中文字体查找路径，支持环境变量 `QUANTLAB_PDF_FONT`、项目字体目录和常见 Linux/macOS 字体路径。
- 补充后端回归测试与前端 Vitest 轻量测试，覆盖本轮关键修复。

## 待后续迭代

- 若需要发行版安装包，可将开源 CJK 字体放入 `data/fonts/` 并在打包脚本中一起分发。
- 前端仍可继续扩展到交互级测试，例如数据中心导入流程和完整回测流程的 Playwright 回归。
