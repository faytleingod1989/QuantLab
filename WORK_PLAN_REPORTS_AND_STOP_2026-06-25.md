# 2026-06-25 报告导出与停止脚本工作计划

## 背景

报告管理页已经支持 HTML/PDF 导出，但 Markdown 后端接口还没有前端入口；同时 `stop.bat` 已存在但未入库，且中文显示存在乱码。

## 本轮目标

- 将 `stop.bat` 修正为可提交、可读、可安全停止本地服务的脚本。
- 在报告管理页补齐 Markdown 下载按钮。
- 完成测试、构建、浏览器验证并推送 GitHub。

## 执行清单

- [x] 修复并纳入 `stop.bat`。
- [x] 报告管理页增加 Markdown 导出入口。
- [x] 运行前端测试、前端构建、后端测试。
- [x] 浏览器验证报告管理页按钮渲染。
- [x] 实测 `stop.bat` 可停止 8000/5173 本地服务。
- [ ] 提交并推送。

## 验证记录

- 前端测试：通过，2 个测试。
- 前端生产构建：通过。
- 后端测试：通过，59 个测试。
- 浏览器交互：报告管理页显示 Markdown / HTML 报告 / PDF 三个导出入口，无控制台错误。
- 停止脚本：`stop.bat` 成功停止本轮启动的 API 与前端服务。
- 已知限制：浏览器插件截图捕获仍出现 CDP 超时，本轮以 DOM 状态、控制台日志和命令输出作为验证证据。
